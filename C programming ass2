#include <stdio.h>

int main() {
    // Write C code here
    int num,i,fact=1;
    scanf("%d",&num);
    i=num;
    while(i>=1)
    {
        fact=fact*i;
        i--;
    }
    printf("the factorial of given number %d is %d\n", num,fact);
}
